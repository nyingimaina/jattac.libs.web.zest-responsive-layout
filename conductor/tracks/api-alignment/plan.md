# Track Plan: Align Props and Types with README

## Objective
Fix discrepancies between the code, the `README.md`, and the `IProps` interface.

## Tasks
- [x] Add `desktopDetailPaneWidth` to `IProps`.
- [x] Deprecate or remove redundant/misleading comments in `README.md`.
- [x] Ensure all exported types are correctly documented.
- [x] Verify build and behavior.

## Atomic Units
1. **Prop Alignment**: Ensure `desktopDetailPaneWidth` is fully supported in the code.
2. **Docs Update**: Synchronize `README.md` with the refactored code (functional component vs PureComponent).
